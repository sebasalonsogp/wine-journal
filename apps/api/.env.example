# Optional for the scaffold. JSON list; match the browser's exact origin.
WINE_JOURNAL_CORS_ORIGINS=["http://localhost:3000"]
# Database/Auth/Storage variables will be added with their integrations.
# Never place backend secrets in NEXT_PUBLIC_* variables.
