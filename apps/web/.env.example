# Reserved for the first API-backed feature. No variables are required yet.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Add the public Supabase URL/publishable key when the sign-in slice starts.
# Only public configuration may use the NEXT_PUBLIC_ prefix.
