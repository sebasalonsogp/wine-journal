# Public routes

Reserve this route group for Browse Wines, public wine details, and Guides. Compose screens from `src/features/`; add `page.tsx` only when a route works. Parentheses do not appear in URLs.
