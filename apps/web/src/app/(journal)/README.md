# Journal routes

Reserve this route group for My Wines and Occasions. My Wines is the initial signed-in destination even though Browse Wines is first in navigation. Route grouping is not an authorization mechanism; private API access must be checked in FastAPI.
