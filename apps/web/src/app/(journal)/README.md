# Journal routes

This route group contains the verified private shell, My Wines/Occasions empty states, and account details. My Wines is the initial signed-in destination even though Browse Wines is first in navigation. Route grouping is not an authorization mechanism; private API access is checked independently in FastAPI. Wine/occasion persistence is a later slice.
