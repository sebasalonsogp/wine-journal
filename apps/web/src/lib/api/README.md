# API contract

`schema.d.ts` is generated from the implemented FastAPI OpenAPI snapshot. Run `npm run generate:api`; never edit generated types manually. The first API-backed feature will add transport and token handling. Planned endpoints remain in `docs/api-contracts.md` until implemented.
