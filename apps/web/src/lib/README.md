# Web infrastructure

Transport, session helpers, and query-client setup belong here when used. Business workflows belong in features. This directory must not import features. The first API-backed slice will add TanStack Query and the authenticated transport boundary.
