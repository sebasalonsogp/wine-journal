# Shared presentation

Reusable presentation such as BottleCover and MediaGallery belongs here when implemented. Keep feature-specific components inside their feature; shared components must not import features or fetch private records themselves.
